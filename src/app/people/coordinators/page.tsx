"use client";

import React from 'react';
import styles from '../people.module.css';

const CoordinatorsPage = () => {
    return (
        <div className={styles.container}>
            <h1 className={styles.pageTitle}>Coordinators</h1>
            <p className={styles.deanDesc} style={{ fontSize: '1.2rem', textAlign: 'center', marginTop: '3rem' }}>
                Content for Coordinators to be added soon.
            </p>
        </div>
    );
};

export default CoordinatorsPage;
